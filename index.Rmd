---
title: "Automation Products for the Integrated Ecosystem Assessment"
editor_options: 
  chunk_output_type: console
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = F)

if (!require("librarian")){
  install.packages("librarian")
}
shelf(
  fs,
  glue,
  htmltools,
  rmarkdown,
  quiet = T)

tagList(html_dependency_font_awesome())
```

Here are all **products** listed alphabetically. Alternatively, you can browse by [Technology](tech.html),  [Region](region.html) or from the menu.

<div class="card-deck">

```{r, results='asis'}
Rmds <- setdiff(list.files(pattern="^p_.*\\.Rmd$"), "index.Rmd")
src = lapply(Rmds, function(Rmd) { # Rmd = Rmds[1]
  knitr::knit_expand('_card.Rmd')
})
res = knitr::knit_child(text = unlist(src), quiet = TRUE)
cat(res, sep = '\n')
```
</div>

<br/>

